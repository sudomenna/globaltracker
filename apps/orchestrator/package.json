{
  "name": "@globaltracker/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "trigger dev",
    "deploy": "trigger deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@globaltracker/db": "workspace:*",
    "@trigger.dev/sdk": "^3.0.0",
    "drizzle-orm": "^0.30.0"
  },
  "devDependencies": {
    "@trigger.dev/build": "^3.0.0",
    "typescript": "^5.4.0"
  }
}
