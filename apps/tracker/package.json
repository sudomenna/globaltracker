{
  "name": "@gt/tracker",
  "version": "0.1.0",
  "private": true,
  "description": "GlobalTracker front-end bundle — installs on LPs via script tag",
  "scripts": {
    "build": "node build.config.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.8.3"
  }
}
