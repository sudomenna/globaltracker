# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Node / pnpm (futuro)
node_modules/
.pnpm-store/
.npm
.yarn/cache
.yarn/install-state.gz
dist/
build/
!tests/build/
.next/
out/
.turbo/

# TypeScript incremental build cache
*.tsbuildinfo

# Env / secrets
.env
.env.local
.env.*.local
.dev.vars
*.key
*.pem

# Wrangler / Cloudflare (futuro)
.wrangler/
wrangler.toml.local

# Supabase CLI runtime
supabase/.temp/

# Drizzle / DB (futuro)
*.sqlite
*.db
drizzle/migrations/.snapshot.json

# Logs
*.log
logs/
npm-debug.log*
pnpm-debug.log*

# Coverage / test artifacts (futuro)
coverage/
.nyc_output/
playwright-report/
test-results/

# Claude Code local cache (não-canônico)
.claude/memory/
.claude/sessions/
.claude/cache/

# Plans dir (local-only)
.claude/plans/

# Local-only Claude Code runtime artifacts
.claude/worktrees/
.claude/scheduled_tasks.lock

# Playwright MCP runtime (snapshots, console logs)
.playwright-mcp/

# Scripts de diagnóstico local — `.mjs` na pasta de manutenção contêm connection
# strings com senha e dumps de PII. Scripts de produção devem ser `.ts`.
scripts/maintenance/*.mjs

# Screenshots de debug do CP (contêm PII em claro)
cp-*.png

# OS
Thumbs.db
